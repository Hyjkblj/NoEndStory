# ============================================
# Environment
# ============================================
# Options: dev, prod, test
ENV=dev

# ============================================
# Database
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=noendstory
DB_USER=postgres
DB_PASSWORD=

# ============================================
# Vector / Embedding
# ============================================
# VECTOR_DB_PATH=
# Options: default, text2vec-chinese, m3e-base, bge-small-zh-v1.5, paraphrase-multilingual
EMBEDDING_MODEL=text2vec-chinese

# ============================================
# AI Provider Keys
# ============================================
OPENAI_API_KEY=

DASHSCOPE_API_KEY=
DASHSCOPE_MODEL=qwen-flash
DASHSCOPE_TTS_MODEL=sambert-zhichu-v1

ZHIPU_API_KEY=
BAIDU_API_KEY=
BAIDU_SECRET_KEY=

# ============================================
# Volcengine (recommended)
# ============================================
VOLCENGINE_ARK_API_KEY=
VOLCENGINE_REGION=cn-beijing

# Unified text model switch (preferred)
LLM_TEXT_MODEL=deepseek-v4-pro-260425
# Legacy key kept for backward compatibility
VOLCENGINE_TEXT_MODEL=

VOLCENGINE_IMAGE_MODEL=doubao-seedream-4-0-250828
VOLCENGINE_IMAGE_SIZE=2K

# 图片生成配置（VectorEngine / OpenAI兼容API）- 优先级高于火山引擎
VECTORENGINE_API_KEY=
VECTORENGINE_BASE_URL=https://api.vectorengine.ai
VECTORENGINE_IMAGE_MODEL=gpt-image-2

# TTS
VOLCENGINE_TTS_APP_ID=
VOLCENGINE_TTS_ACCESS_TOKEN=
VOLCENGINE_TTS_SECRET_KEY=
VOLCENGINE_TTS_MODEL=seed-tts-2.0
VOLCENGINE_TTS_RESOURCE_ID=volc.tts.default

# Optional WebSocket TTS
VOLCENGINE_TTS_WEBSOCKET_URL=wss://openspeech.bytedance.com/api/v3/tts/bidirection
VOLCENGINE_TTS_USE_WEBSOCKET=false

# TTS feature flags
VOLCENGINE_TTS_ENABLE_TIMESTAMP=false
VOLCENGINE_TTS_ENABLE_CACHE=true
VOLCENGINE_TTS_ENABLE_EMOTION=true

# ============================================
# TTS Provider
# ============================================
# Options: volcengine, dashscope, edge-tts
TTS_PROVIDER=volcengine

# ============================================
# Optional local model (Ollama)
# ============================================
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=qwen:7b

# ============================================
# Optional game tuning
# ============================================
# GAME_MAX_EVENTS=3
# GAME_TOTAL_EVENTS=5

# ============================================
# Image output
# ============================================
IMAGE_SAVE_DIR=./images/characters
SCENE_IMAGE_SAVE_DIR=./images/scenes
SMALL_SCENE_IMAGE_SAVE_DIR=./images/smallscenes
COMPOSITE_IMAGE_SAVE_DIR=./images/composite
IMAGE_SAVE_ENABLED=true

# ============================================
# CORS (required in production)
# ============================================
# ALLOWED_ORIGINS=https://example.com,https://www.example.com

# ============================================
# Logging
# ============================================
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# Options: plain, json (json recommended for production)
LOG_FORMAT=plain

# ============================================
# Security (W4)
# ============================================
# Rate limiting
RATE_LIMIT_DEFAULT_MAX=100
RATE_LIMIT_WINDOW_SECONDS=3600
GUEST_FREE_PLAYS=3

# Cost guard
COST_LIMIT_PER_IP_HOURLY=2.0
COST_LIMIT_PER_IP_DAILY=5.0

# ============================================
# 引擎选择（优先级: ScriptEngine V2 > NOS Agent > Dynamic Script > Legacy）
# ============================================
# USE_SCRIPT_ENGINE_V2=true     # ScriptEngine V2（新默认，推荐）
# USE_NOS_AGENT_ENGINE=false    # NOS Agent 引擎（旧版，仅在 V2 禁用时生效）
# USE_DYNAMIC_SCRIPT=false      # 动态剧本系统（旧版，仅在 V2 禁用时生效）

# ============================================
# LLM Call Parameters (可选，覆盖默认值)
# ============================================
# Format: LLM_CALL_{TYPE}_{PARAM}
# Types: STORY, DIALOGUE, OPTIONS, SUPPLEMENT, EVENT_CONTEXT, SCENE_INFERENCE, CONTINUE_CHECK
# LLM_CALL_DIALOGUE_MAX_TOKENS=100
# LLM_CALL_DIALOGUE_TEMPERATURE=0.9
# LLM_CALL_STORY_MAX_TOKENS=300
# LLM_CALL_STORY_TEMPERATURE=0.8
# LLM_CALL_OPTIONS_MAX_TOKENS=150
# LLM_MAX_RETRIES=3
# LLM_RETRY_DELAY=1.0

# ============================================
# Token Budget (W11 — 防止恶意消耗)
# ============================================
# Per-IP daily token limit (default: 50000 ≈ $0.05/day with deepseek)
TOKEN_DAILY_PER_IP=50000
# Global daily token limit (default: 500000 ≈ $0.50/day)
TOKEN_DAILY_GLOBAL=500000

# ============================================
# Observability (W11)
# ============================================
# HEALTH_FULL_CHECK=false
